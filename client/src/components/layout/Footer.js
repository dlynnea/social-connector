import React from 'react';

const Footer = () => <footer className='footer'>
        <p> dot join &copy; {new Date().getFullYear()}</p>
        {/* <p>[ A JOIN is a means for combining columns (people) from one or more tables (backgrounds) by using values (interests) common to each ]</p> */}
</footer>

export default Footer;
